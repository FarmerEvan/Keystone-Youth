---
import BaseLayout from '../layouts/BaseLayout.astro';
import VolunteerOpportunities from '../components/VolunteerOpportunities.astro';
import PrayerWall from '../components/PrayerWall.astro';

const eventsCsv = import.meta.env.PUBLIC_EVENTS_CSV_URL;
const scriptAppUrl = import.meta.env.PUBLIC_SCRIPT_URL; 
---

<BaseLayout title="Student Hub" backLink="/" backText="Home" accentColor="#fb923c">
    
    <div class="header" style="text-align: center; margin-bottom: 3rem;">
        <h1 style="font-size: 3rem; color: #fb923c; margin: 0 0 10px 0;">Student Hub</h1>
        <div class="memory-verse" style="background: rgba(56, 189, 248, 0.1); border-left: 4px solid #38bdf8; padding: 1.5rem; border-radius: 0 12px 12px 0; max-width: 700px; margin: 0 auto; text-align: left;">
            <h3 style="color: #38bdf8; margin: 0 0 10px 0; font-size: 1.1rem; text-transform: uppercase; letter-spacing: 1px;">April Memory Verse</h3>
            <p style="font-size: 1.2rem; font-style: italic; margin: 0 0 10px 0; line-height: 1.6;">"Don't let anyone look down on you because you are young, but set an example for the believers in speech, in conduct, in love, in faith and in purity."</p>
            <p class="reference" style="font-size: 1rem; font-weight: bold; color: #cbd5e1; text-align: right; margin: 0;">- 1 Timothy 4:12</p>
        </div>
    </div>

    <div id="today-events-section" style="display: none; margin-bottom: 2rem;">
        <div class="card" style="background: rgba(239, 68, 68, 0.1); border: 2px solid #ef4444; border-radius: 16px; padding: 2rem; box-shadow: 0 10px 25px rgba(239, 68, 68, 0.2);">
            <h2 style="display: flex; align-items: center; gap: 10px; margin-top: 0; color: #ef4444; border-bottom: 1px solid rgba(239, 68, 68, 0.2); padding-bottom: 10px;">🚨 Happening Today!</h2>
            <div id="today-events-container"></div>
        </div>
    </div>

    <div class="grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem;">
        
        <div class="card" style="background: rgba(255, 255, 255, 0.05); border: 1px solid rgba(255, 255, 255, 0.1); border-radius: 16px; padding: 2rem; display: flex; flex-direction: column;">
            <h2 style="display: flex; align-items: center; gap: 10px; margin-top: 0; color: #f8fafc; border-bottom: 1px solid rgba(255, 255, 255, 0.1); padding-bottom: 10px;">🍕 Upcoming Events</h2>
            <p style="color: #cbd5e1;">Click an event for details, resources, and photos!</p>
            <div id="live-events-container"><p style="text-align: center; color: #94a3b8; font-style: italic;">Loading events...</p></div>
        </div>

        <div class="card" style="background: rgba(255, 255, 255, 0.05); border: 1px solid rgba(255, 255, 255, 0.1); border-radius: 16px; padding: 2rem; display: flex; flex-direction: column;">
            <h2 style="display: flex; align-items: center; gap: 10px; margin-top: 0; color: #3b82f6; border-bottom: 1px solid rgba(255, 255, 255, 0.1); padding-bottom: 10px;">🙏 Prayer Wall</h2>
            <p style="color: #cbd5e1; margin-bottom: 20px;">Bear one another's burdens. See how you can pray for our ministry.</p>
            
            <PrayerWall audience="Student" />
        </div>

        <div class="card" style="background: rgba(255, 255, 255, 0.05); border: 1px solid rgba(255, 255, 255, 0.1); border-radius: 16px; padding: 2rem; display: flex; flex-direction: column;">
            <h2 style="display: flex; align-items: center; gap: 10px; margin-top: 0; color: #a855f7; border-bottom: 1px solid rgba(255, 255, 255, 0.1); padding-bottom: 10px;">🤝 Student Leadership & Serving</h2>
            <p style="color: #cbd5e1; margin-bottom: 20px;">Ready to step up? See where you can serve and lead within the youth group.</p>
            
            <VolunteerOpportunities audience="Student" />
        </div>

    </div>

    <div id="event-view-modal" class="modal-overlay" style="display: none; position: fixed; inset: 0; background: rgba(15, 23, 42, 0.85); backdrop-filter: blur(5px); z-index: 10000; align-items: center; justify-content: center; padding: 1rem;">
        <div class="modal-content" style="background: #1e293b; border-top: 4px solid #fb923c; border-radius: 16px; width: 100%; max-width: 600px; max-height: 90vh; overflow-y: auto; padding: 2rem; box-shadow: 0 20px 50px rgba(0,0,0,0.8);">
            
            <h2 id="modal-ev-title" style="color: white; margin: 0 0 5px 0;">Event Title</h2>
            <p id="modal-ev-time" style="color: #fb923c; font-weight: bold; margin: 0 0 15px 0;">Date & Time</p>
            
            <div id="modal-ev-desc" style="color: #cbd5e1; line-height: 1.6; margin-bottom: 20px;"></div>
            <div id="modal-ev-links" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 15px; margin-bottom: 30px;"></div>

            <div style="background: rgba(0,0,0,0.2); border-radius: 12px; padding: 1.5rem; border: 1px solid rgba(255,255,255,0.05); margin-bottom: 20px;">
                <h3 style="margin: 0 0 10px 0; color: #f8fafc; font-size: 1.1rem;">💬 Event Feedback</h3>
                <p style="margin: 0 0 15px 0; color: #94a3b8; font-size: 0.85rem;">What did you like? What could we do better next time?</p>
                <textarea id="feedback-text" rows="3" placeholder="Type your thoughts here..." style="width: 100%; padding: 10px; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1); background: rgba(0,0,0,0.3); color: white; font-family: inherit; margin-bottom: 10px; resize: vertical; box-sizing: border-box;"></textarea>
                <button id="btn-submit-feedback" style="background: #10b981; color: white; border: none; padding: 10px 15px; border-radius: 8px; font-weight: bold; cursor: pointer; width: 100%; transition: opacity 0.2s;">Submit Feedback</button>
            </div>

            <div style="text-align: right;">
                <button onclick="document.getElementById('event-view-modal').style.display='none'" style="background: transparent; border: 1px solid #475569; color: white; padding: 10px 20px; border-radius: 8px; cursor: pointer;">Close Window</button>
            </div>
        </div>
    </div>

</BaseLayout>

<script is:inline define:vars={{ CSV_URL: eventsCsv, SCRIPT_URL: scriptAppUrl }}>
    let currentFeedbackContext = "";

    function parseCsvRow(row, expectedLength) {
        const c = []; let inQuotes = false; let current = '';
        for (let i = 0; i < row.length; i++) {
            const char = row[i];
            if (char === '"') inQuotes = !inQuotes;
            else if (char === ',' && !inQuotes) { c.push(current); current = ''; }
            else current += char;
        }
        c.push(current);
        const mapped = c.map(s => s.replace(/^"|"$/g, '').trim());
        while(mapped.length < expectedLength) mapped.push('');
        return mapped;
    }

    function formatDate(raw) {
        if (!raw) return '';
        if (raw.match(/^\d{4}-\d{2}-\d{2}$/)) {
            const p = raw.split('-');
            const d = new Date(p[0], p[1]-1, p[2]);
            return d.toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric', year: 'numeric' });
        }
        try {
            const d = new Date(raw);
            if (!isNaN(d)) return d.toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric', year: 'numeric' });
        } catch(e) {}
        return raw;
    }

    function formatTime(raw) {
        if (!raw) return '';
        const str = raw.trim();
        const match = str.match(/(\d{1,2}):(\d{2})(?::\d{2})?\s*(AM|PM|am|pm)?/i);
        if (match) {
            let h = parseInt(match[1], 10);
            let m = match[2];
            let ampm = match[3] ? match[3].toLowerCase() : null;
            if (!ampm) { ampm = h >= 12 ? 'pm' : 'am'; h = h % 12 || 12; }
            return `${h}:${m}${ampm}`;
        }
        return str;
    }

    function getEventTimestamp(dateStr, timeStr, isEnd = false) {
        if (!dateStr) return 0;
        let d = new Date();
        if (dateStr.match(/^\d{4}-\d{2}-\d{2}$/)) {
            const p = dateStr.split('-');
            d = new Date(p[0], p[1]-1, p[2]);
        } else {
            d = new Date(dateStr);
            if (isNaN(d)) return 0;
        }
        if (timeStr) {
            const match = timeStr.trim().match(/(\d{1,2}):(\d{2})(?::\d{2})?\s*(AM|PM|am|pm)?/i);
            if (match) {
                let h = parseInt(match[1], 10);
                let m = parseInt(match[2], 10);
                let ampm = match[3] ? match[3].toLowerCase() : null;
                if (ampm === 'pm' && h < 12) h += 12;
                if (ampm === 'am' && h === 12) h = 0;
                d.setHours(h, m, 0, 0);
            } else { 
                if (isEnd) d.setHours(23, 59, 59, 999); else d.setHours(0,0,0,0);
            }
        } else { 
            if (isEnd) d.setHours(23, 59, 59, 999); else d.setHours(0,0,0,0);
        }
        return d.getTime();
    }

    function isToday(rawDate) {
        if(!rawDate) return false;
        let d;
        if (rawDate.match(/^\d{4}-\d{2}-\d{2}$/)) {
            const p = rawDate.split('-');
            d = new Date(p[0], p[1]-1, p[2]);
        } else { d = new Date(rawDate); }
        if (isNaN(d)) return false;
        
        const today = new Date();
        return d.getDate() === today.getDate() && d.getMonth() === today.getMonth() && d.getFullYear() === today.getFullYear();
    }

    function addViewAllButton(container, hiddenElements, totalCount, color) {
        const btn = document.createElement('button');
        btn.innerText = `View All (${totalCount}) ▾`;
        btn.style = `width: 100%; background: transparent; border: 1px dashed ${color}; color: ${color}; padding: 10px; border-radius: 8px; font-weight: bold; cursor: pointer; transition: all 0.2s; margin-top: 5px;`;
        let isExpanded = false;
        btn.onmouseover = () => { btn.style.background = 'rgba(255,255,255,0.05)'; };
        btn.onmouseout = () => { btn.style.background = 'transparent'; };
        btn.onclick = () => {
            isExpanded = !isExpanded;
            hiddenElements.forEach(el => el.style.display = isExpanded ? el.dataset.originalDisplay : 'none');
            btn.innerText = isExpanded ? `Show Less ▴` : `View All (${totalCount}) ▾`;
        };
        container.appendChild(btn);
    }

    async function loadHubEvents() {
        const fetchT = async (url) => { if(!url) return ""; const res = await fetch(`${url}${url.includes('?') ? '&' : '?'}t=${Date.now()}`); return await res.text(); };
        try {
            const evData = await fetchT(CSV_URL);
            
            const rows = evData.split('\n').slice(1);
            const liveContainer = document.getElementById('live-events-container');
            const todayContainer = document.getElementById('today-events-container');
            liveContainer.innerHTML = '';
            todayContainer.innerHTML = '';
            
            const now = Date.now();
            let parsedEvents = [];

            rows.forEach(row => {
                if(!row.trim()) return;
                const c = parseCsvRow(row, 11);
                if (!c[4]) return; 

                const endTimestamp = getEventTimestamp(c[1], c[3] || c[2], true);
                const isPast = now > endTimestamp;

                const ev = { 
                    id: c[0],
                    date: c[1], timeStart: c[2], timeEnd: c[3], title: c[4], 
                    desc: c[5].replace(/\\n/g, '<br>'), high: (c[6] || '').toUpperCase() === 'TRUE', 
                    photo: c[7], brochure: c[8], handout: c[9], leader: c[10],
                    isPast: isPast,
                    timestampStart: getEventTimestamp(c[1], c[2], false)
                };
                parsedEvents.push(ev);
            });

            parsedEvents.sort((a, b) => a.timestampStart - b.timestampStart);

            let liveCount = 0; let hiddenLive = [];
            let todayCount = 0; let hiddenToday = [];

            parsedEvents.forEach(ev => {
                const isTodayStr = isToday(ev.date);
                if (ev.isPast) return; 

                const timeString = ev.timeEnd ? `${formatTime(ev.timeStart)} - ${formatTime(ev.timeEnd)}` : formatTime(ev.timeStart);
                const box = document.createElement('div');
                const borderColor = isTodayStr ? '#ef4444' : (ev.high ? '#fb923c' : '#475569');
                const dateColor = isTodayStr ? '#ef4444' : (ev.high ? '#fb923c' : '#94a3b8');

                box.style = `background: rgba(0, 0, 0, ${ev.high || isTodayStr ? '0.2' : '0.1'}); border-radius: 8px; padding: 15px; margin-bottom: 15px; border-left: 4px solid ${borderColor}; cursor: pointer; transition: transform 0.2s;`;
                box.onmouseover = () => box.style.transform = "translateX(5px)";
                box.onmouseout = () => box.style.transform = "translateX(0)";
                
                box.onclick = () => {
                    const links = [
                        {url: ev.brochure, title: "Take-Home Brochure", subtitle: "Event details & info", icon: "📄", color: "#fb923c"},
                        {url: ev.photo, title: "Shared Photo Album", subtitle: "View & upload memories", icon: "📸", color: "#38bdf8"},
                        {url: ev.handout, title: "Lesson Handout", subtitle: "Follow along", icon: "✍️", color: "#facc15"}
                    ];
                    openEventModal(ev.title, `${formatDate(ev.date)} • ${timeString}`, ev.desc, links, `[ID:${ev.id}]`);
                };

                box.innerHTML = `
                    <div style="color: ${dateColor}; font-weight: bold; font-size: 0.85rem; text-transform: uppercase; margin-bottom: 5px;">${isTodayStr ? 'TODAY' : formatDate(ev.date)}</div>
                    <div style="color: #facc15; font-size: 0.8rem; margin-bottom: 5px;">${timeString}</div>
                    <h3 style="margin: 0 0 5px 0; color: #fff;">${ev.title}</h3>
                `;
                
                if (isTodayStr) {
                    if (todayCount >= 3) { box.dataset.originalDisplay = box.style.display || 'block'; box.style.display = 'none'; hiddenToday.push(box); }
                    todayContainer.appendChild(box);
                    todayCount++;
                } else {
                    if (liveCount >= 3) { box.dataset.originalDisplay = box.style.display || 'block'; box.style.display = 'none'; hiddenLive.push(box); }
                    liveContainer.appendChild(box);
                    liveCount++;
                }
            });

            if (todayCount > 0) {
                document.getElementById('today-events-section').style.display = 'block';
                if (hiddenToday.length > 0) addViewAllButton(todayContainer, hiddenToday, todayCount, '#ef4444');
            }
            if (liveCount === 0) liveContainer.innerHTML = '<p style="color: #94a3b8;">No upcoming events at this time.</p>';
            else if (hiddenLive.length > 0) addViewAllButton(liveContainer, hiddenLive, liveCount, '#fb923c');

        } catch (error) {
            document.getElementById('live-events-container').innerHTML = '<p style="color: #ef4444;">Failed to load events.</p>';
        }
    }

    function openEventModal(title, subtitle, desc, linksConfig, feedbackContext) {
        currentFeedbackContext = feedbackContext;
        document.getElementById('modal-ev-title').innerText = title;
        document.getElementById('modal-ev-time').innerText = subtitle;
        document.getElementById('modal-ev-desc').innerHTML = desc;
        document.getElementById('feedback-text').value = ''; 
        document.getElementById('btn-submit-feedback').innerText = 'Submit Feedback';

        const linksContainer = document.getElementById('modal-ev-links');
        linksContainer.innerHTML = '';

        const linkHTML = linksConfig.map(l => {
            if(!l.url || l.url.trim() === '') return '';
            return `
                <a href="${l.url}" target="_blank" style="display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; gap: 8px; background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); text-decoration: none; padding: 20px 15px; border-radius: 12px; transition: all 0.2s;" onmouseover="this.style.background='rgba(255,255,255,0.1)'; this.style.transform='translateY(-3px)'" onmouseout="this.style.background='rgba(255,255,255,0.05)'; this.style.transform='translateY(0)'">
                    <span style="font-size: 2rem;">${l.icon}</span>
                    <strong style="color: ${l.color}; font-size: 1rem; margin-top: 5px;">${l.title}</strong>
                    <span style="color: #94a3b8; font-size: 0.75rem;">${l.subtitle}</span>
                </a>
            `;
        }).filter(Boolean).join('');

        linksContainer.innerHTML = linkHTML;
        document.getElementById('event-view-modal').style.display = 'flex';
    }

    document.getElementById('btn-submit-feedback').onclick = async () => {
        const text = document.getElementById('feedback-text').value.trim();
        if(!text) return alert("Please type your feedback first!");
        
        const btn = document.getElementById('btn-submit-feedback');
        btn.innerText = "Sending...";
        
        try {
            const params = new URLSearchParams({ action: "submitFeedback", event: currentFeedbackContext, feedback: text });
            await fetch(`${SCRIPT_URL}?${params.toString()}`, {mode:'no-cors'});
            btn.innerText = "✅ Sent!";
            setTimeout(() => { document.getElementById('event-view-modal').style.display = 'none'; }, 2000);
        } catch (e) {
            btn.innerText = "❌ Error sending";
        }
    };

    loadHubEvents();
    document.addEventListener('astro:page-load', loadHubEvents);
</script>